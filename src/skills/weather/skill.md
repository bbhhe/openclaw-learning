# Weather Skill

To check the weather, use the `exec` tool with `curl`.

## Usage

- Current weather: `curl -s wttr.in/Shanghai?format=3`
- Forecast: `curl -s wttr.in/Shanghai`

Replace "Shanghai" with the user's requested location.
